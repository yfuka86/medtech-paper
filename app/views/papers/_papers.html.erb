<h1 class='papers-title'><%= title %></h1>
<div class='papers'>
  <% if !(papers.length == 0) %>
    <%= render 'papers/papers_header' %>
    <% papers.each do |p| %>
      <%= render 'papers/paper', paper: p, user: user %>
    <% end %>
    <div class='end-line'></div>
  <% else %>
    <p>お探しの論文は見つかりませんでした</p>
  <% end %>
</div>
